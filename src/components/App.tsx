import React from 'react';
import { hot } from 'react-hot-loader/root';

const App: React.FC = () => {
    return (
        <div>
            App here !
        </div>
    );
};

export default hot(App);
